export interface WAFunctions {
	version(): number,
    //createBuffer(width: number, height: number): ArrayBuffer,
	//encode(buffer: ArrayBuffer, width: number, height: number, quality: number): void,
	//freeResult(pointer: number): void,
	//getResultPointer(): number,
	//getResultSize(): number
}
